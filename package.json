{
  "name": "vue-columns-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/vue-columns"
  ],
  "scripts": {
    "build": "lerna run build",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
